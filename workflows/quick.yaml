name: quick
description: |
  Дёшево, для тривиальной рутины: только implement, без ревью.
  Модели берутся из карты ролей Settings (role → model), а не хардкожены.
  ⚠️ Результат НЕ идёт в main без ручной проверки (раннер помечает ветку
  needs-human-review).
steps:
  - id: implement
    role: implement
    effort: low
    budget:
      wall_time_sec: 600
      max_steps: 2
    depends_on: []
    target_paths: []
