name: quick
description: |
  Дёшево, для тривиальной рутины: только GLM-имплемент, без ревью.
  ⚠️ Результат НЕ идёт в main без ручной проверки (раннер помечает ветку
  needs-human-review). PLAN §3.2.
steps:
  - id: implement
    agent: glm
    role: implement
    effort: low
    budget:
      wall_time_sec: 600
      max_steps: 2
    depends_on: []
    target_paths: []
