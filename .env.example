# Шаблон — скопировать в .env.local и заполнить своими кредами.
# .env.local в .gitignore и НЕ коммитится.

# GLM (Z.ai) — для воркфлоу с glm-шагами.
GLM_BASE_URL=https://api.z.ai/api/anthropic
GLM_API_KEY=your-zai-api-key-here

# Ollama (local executor, optional — only for workflows using ollama steps).
# runOllama / checkOllama читают эти переменные напрямую из process.env.
OLLAMA_BASE_URL=http://d0nlebed0n.tail74ba62.ts.net:11434
OLLAMA_MODEL=danielsheep/Qwen3-Coder-30B-A3B-Instruct-1M-Unsloth:UD-IQ3_XXS
